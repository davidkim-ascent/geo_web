# Dev Log

## 2026-05-12

폼 이름의 의미를 분리해서 기록합니다.

- `contact_form`: 서비스 문의 / 상담 요청용 폼
- `download_form`: 화이트페이퍼 다운로드 요청용 폼

현재 구현에서는 하나의 `DownloadForm` 컴포넌트가 `formName` 값에 따라 두 역할을 모두 처리합니다.
- `formName="contact_form"`이면 컨택 폼 문구와 짧은 상담용 입력 흐름을 사용합니다.
- 그 외 값이면 다운로드 폼 문구와 화이트페이퍼 요청 흐름을 사용합니다.
