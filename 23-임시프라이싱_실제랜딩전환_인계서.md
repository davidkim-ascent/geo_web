# 23. 목업 프라이싱 → 실제 랜딩 전환 인계서

작성일: 2026-08-17

## 배경

GEO-Shindan(`/pricing`, `https://geo-shindan.ascentnet.co.jp/pricing`)과 GEO-Watcher(`/pricing`, `https://geo-watcher.ascentnet.co.jp/pricing`)는 각 앱 저장소 안에 **임시 스탠드인 랜딩**을 갖고 있다. 두 파일 모두 코드 주석에 명시적으로 "정식 마케팅 LP가 생기기 전까지의 임시 페이지"라고 적혀 있다.

- `GEO-Shindan/apps/shindan-web/src/routes/pricing.tsx`
- `GEO-Watcher/apps/web/src/routes/pricing.tsx`

이제 `geo-web` 프로젝트에 정식 마케팅 랜딩(`/shindan`, `/watcher`)이 완성되어 있으므로, 이 임시 페이지들을 걷어내고 실제 방문자를 `geo-web`의 랜딩으로 유도해야 한다.

## 현재 상태

| 항목 | GEO-Shindan | GEO-Watcher |
|---|---|---|
| 임시 페이지 | `/pricing` (`STRIPE_PLANS` 그대로 렌더링, UI 최소) | `/pricing` (동일 구조) |
| 정식 랜딩(geo-web) | `src/app/shindan/ShindanPricingSection.tsx` | `src/app/watcher/PricingSection.tsx` |
| 정식 랜딩의 "이 플랜으로 시작" 버튼 | 이미 `https://geo-shindan.ascentnet.co.jp/signup?plan=...`로 연결됨(확인 완료) | 이미 `https://geo-watcher.ascentnet.co.jp/signup?plan=...&annual=...&promptAddonEnabled=...`로 연결됨(확인 완료) |
| 가격/플랜 데이터 정합성 | 정식 랜딩의 가격(¥19,800/45,000/80,000)이 실제 라이브 Price와 일치(19번 문서 기준 확인) | 정식 랜딩의 가격(¥29,800/39,800/79,800 등)이 실제 라이브 Price와 일치 |

즉 **geo-web 쪽 랜딩 자체는 이미 실제 앱의 `/signup`으로 정확히 연결되어 있어 추가 작업이 필요 없다.** 문제는 반대 방향 — 앱 저장소 안의 **임시 `/pricing` 페이지가 여전히 살아있고, 코드 내부에서 계속 참조되는 경로**라는 점이다.

## 임시 페이지를 참조하는 지점

| 위치 | 참조 내용 |
|---|---|
| `GEO-Shindan/apps/shindan-web/src/routes/signup.tsx:71` | 가입 취소 시 `navigate({ to: "/pricing" })`로 자체 임시 페이지로 되돌아감 |
| `GEO-Shindan/apps/shindan-web/src/server/billing.ts` | Stripe Checkout의 `cancelUrl`이 `${appUrl}/pricing`을 가리킴(10번 문서 T10-3에서 확정된 값) |
| `GEO-Watcher/apps/web/src/routes/pricing.tsx` | 동일 구조, 자체 참조 없음(확인 필요 — 아래 작업 항목 참고) |

## geo-web 배포 도메인 (확정)

- `https://geo.ascentnet.co.jp/watcher`
- `https://geo.ascentnet.co.jp/shindan`

**단, 이 문서 작성 시점(2026-08-17) 기준 geo-web은 아직 실서버 배포 전이다.** 아래 작업은 **geo-web이 이 도메인으로 실제 배포 완료된 이후에** 진행해야 한다 — 앱 쪽 링크를 먼저 바꿔버리면 아직 존재하지 않는 도메인으로 방문자를 보내게 되어 더 나쁜 상태가 된다.

## 작업 항목 (순서대로)

- [ ] **0단계(선행 조건)**: geo-web을 `https://geo.ascentnet.co.jp`로 실서버 배포 완료, `/watcher`·`/shindan` 정상 접속 확인
- [ ] **GEO-Shindan** `apps/shindan-web/src/server/billing.ts`의 Stripe Checkout `cancelUrl`을 `${appUrl}/pricing` → `https://geo.ascentnet.co.jp/shindan`으로 교체
- [ ] **GEO-Shindan** `apps/shindan-web/src/routes/signup.tsx:71`의 `navigate({ to: "/pricing" })`을 `https://geo.ascentnet.co.jp/shindan`으로 이동하는 외부 링크(`window.location.href` 등)로 교체
- [ ] **GEO-Watcher**도 동일하게 Stripe Checkout `cancelUrl`과 `signup.tsx`(또는 대응 파일)의 `/pricing` 참조를 `https://geo.ascentnet.co.jp/watcher`로 교체
- [ ] 위 참조 지점을 모두 교체한 뒤, GEO-Shindan/GEO-Watcher 두 앱의 `/pricing` 라우트 파일(`routes/pricing.tsx`) 자체를 삭제
- [ ] `/pricing` 삭제 후에도 기존에 이 URL을 북마크했거나 외부에 공유된 링크가 있을 수 있으므로, 완전 삭제 대신 `https://geo.ascentnet.co.jp/{shindan|watcher}`로 302 리다이렉트하는 최소한의 라우트로 남겨두는 것을 권장
- [ ] geo-web 쪽(`ShindanPricingSection.tsx`, `PricingSection.tsx`)은 **추가 수정 불필요** — 이미 실제 앱 `/signup`으로 정확히 연결되어 있음(플랜/annual/promptAddonEnabled 파라미터까지 일치 확인됨)

## 확인이 필요한 사항

- geo-web 실서버 배포 일정 — 위 작업 전부가 이 배포 완료를 전제로 함
- `/pricing`을 완전 삭제할지, 리다이렉트만 남길지 최종 결정 (권장: 리다이렉트 유지)
